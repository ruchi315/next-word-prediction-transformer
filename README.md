# Next Word Prediction System

A Transformer-based next word prediction system built using PyTorch.

The model learns language patterns from WikiText-2 and predicts possible next words based on user input.

The project includes:
- Custom tokenizer
- Transformer decoder architecture
- Multi-head self-attention
- Positional encoding
- FastAPI backend
- Streamlit interactive UI